@using WebBanHang.Models
<div class="header-top">
    <div class="container">
        <div class="search">
            @using (Html.BeginForm("Search","Home",FormMethod.Get))
            {
                <input type="text" name="searchString" placeholder="Nhập tên cần tìm " />
                <input type="submit" value="Tìm">
            }
            @*<form>
            <input type="text" value="Search " onfocus="this.value = '';" onblur="if (this.value == '') {this.value = 'Search';}">
            <input type="submit" value="Go">
        </form>*@
        </div>
        <div class="header-left">
            <ul>
                @if (Session["TaiKhoan"] != null && Session["TaiKhoan"].ToString() != "")
                {
                    ThanhVien thanhvien = (ThanhVien)Session["TaiKhoan"];
                    <liv>Xin chào @thanhvien.HoTen</liv>
                    @Html.ActionLink("Đăng Xuất ", "Logout", "Home")

                }
                else
                {
                    <li>@Html.ActionLink("Đăng nhập", "Login", "Home")</li>
                }

                <li>@Html.ActionLink("Đăng ký", "Register", "Home")</li>

            </ul>
            @* Chèn partial view giỏ hàng *@

            @Html.Action("GioHangPartial", "GioHang")

           
            <div class="clearfix"> </div>
        </div>
        <div class="clearfix"> </div>
    </div>
</div>
